{
  "name": "myapp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "test": "mocha 'test/**/*.js'" // Ensure it matches the test file pattern
  },
  "engines": {
    "node": ">=16.14.0" // Adjust to a version that supports the updated packages
  },
  "dependencies": {
    "express": "^4.18.2", // Updated version
    "http-errors": "^2.0.0", // Updated version
    "mysql2": "^4.2.0" // Updated version
  },
  "devDependencies": {
    "mocha": "^10.6.0", // Updated version
    "mkdirp": "^2.1.0", // Updated version
    "inherits": "^2.0.4", // Updated version
    "ms": "^2.1.3" // Updated version
  }
}
